package katas.lettercombos

/**
  * I/P:     A String array: ["A", "B", "C", "D"]  (N array of unit length strings)
  * O/P:  A String array with the combination of strings from left to right e.g:
  * ["A", "AB", "ABC", "ABCD", "B", "BC", "BCD", "C", "CD", "D"]
  * (Order doesn't matter here. But it should have all possible combinations from left to right)
  */
object LetterCombos {

  def buildCombos(letters: Seq[Char]) = {
    //def build(prefix: String, rem: Seq[])
  }
}
